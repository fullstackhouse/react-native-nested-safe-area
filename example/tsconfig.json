{
  "extends": "../tsconfig",
  "compilerOptions": {
    // Avoid expo-cli auto-generating a tsconfig
    "paths": {
      "@fullstackhouse/react-native-nested-safe-area": ["../src/index"]
    }
  }
}
